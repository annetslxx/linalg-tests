Набор автоматических тестов для библиотеки linalg, написанный с использованием **GoogleTest**.  
Тесты проверяют корректность всех основных операций с матрицами: арифметику, методы Гаусса, вычисление определителя, ранга, нормы, обращение и решение СЛАУ.
Написано для разработчиков библиотеки linalg.

## Автор

**Долгополова Анна Руслановна**  
Студентка 2 курса образовательной программы "Прикладная математика"  
НИУ ВШЭ  
📧 ardolgopolova@edu.hse.ru

## Требования

- C++17
- CMake 3.22+
- GoogleTest
- Библиотека `linalg` (подмодуль)

## Сборка и запуск

### 1. Склонируйте основной проект с подмодулем linalg

``` bash
git clone --recurse-submodules https://git.miem.hse.ru/lab-1/linalg-tests.git
cd linalg_tests
```

### 2. Соберите проект

``` bash
mkdir build && cd build
cmake ..
cmake --build .
```

### 3. Запустите тесты
``` bash 
./tests
```


## Структура проекта

``` bash
linalg_tests/
│
├── CMakeLists.txt        # Сборка тестов и подключение GoogleTest
├── source/
│   ├── constructors_tests.cpp
│   ├── methods_tests.cpp
│   ├── operators_tests.cpp
│   └── computation_tests.cpp
│
├── linalg/               # Подмодуль с реализацией библиотеки Matrix
└── googletest/           # Подключённая библиотека GoogleTest

```


## Что проверяют тесты

### constructors_tests.cpp (конструкторы)

##### Проверяемые возможности:
Конструкторы: дефолтный, с параметрами (одним параметром), копирования, перемещения, инициализация списками.

### methods_tests.cpp (методы класса Matrix)

##### Проверяемые возможности:
Методы: reshape, reserve, clear, shrink_to_fit, swap.

### operators_tests.cpp (арифметические операторы)

##### Проверяемые возможности:
Операторы: копирования, перемещения, +, - (унарные и бинарные), сложение и вычитание матриц, их перемножение, умножение матрицы на число, +=, -=, *=, == и !=, а также [] и ().

### computation_tests.cpp
##### Проверяемые возможности:
Проверка работоспособности следующих функций: norm, trace, rank, invert, solve, power, transpose, concatenate, а также проверка на граничные случаи и выброс исключений.


## Пример теста

``` cpp
TEST(MatrixTest, DetBasic) {
    Matrix m{{1.0, 2.0}, {3.0, 4.0}};
    EXPECT_NEAR(m.det(), -2.0, 1e-9);
}
```
Все тесты используют макрос EXPECT_* из GoogleTest.

---

**Версия документа**: 1.0  
**Последнее обновление**: Октябрь 2025